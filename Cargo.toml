[package]
name = "ratatui-interact-fork"
version = "0.5.3-fork.1"
edition = "2024"
rust-version = "1.85"
description = "A fork of the ratatui-interactive crate with some patches"
license = "MIT"
authors = ["Leon Degel-Köhn", "Brainwires Team"]
keywords = ["tui", "ratatui", "widgets", "focus", "mouse"]
categories = ["command-line-interface", "gui"]
repository = "https://github.com/keyvizsla/ratatui-interact"
homepage = "https://github.com/keyvizsla/ratatui-interact"
documentation = "https://docs.rs/ratatui-interact"
readme = "README.md"

[dependencies]
# TUI framework
ratatui = { version = "0.30", features = ["unstable-rendered-line-info"] }
crossterm = "0.29"

# Error handling
thiserror = "2.0"

# Text processing
regex = "1.12"
unicode-width = "0.2"

# Optional: Markdown rendering
termimad = { version = "0.34", optional = true }

# Optional: Clipboard support
arboard = { version = "3.4", optional = true }

# Optional: Serialization support for themes
serde = { version = "1", features = ["derive"], optional = true }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }

[features]
default = []
# Enable markdown rendering in ANSI parser
markdown = ["dep:termimad"]
# Enable filesystem operations in file explorer
filesystem = []
# Enable clipboard copy/paste support
clipboard = ["dep:arboard"]
# Enable serde serialization/deserialization for themes
theme-serde = ["dep:serde", "ratatui/serde"]

[[example]]
name = "explorer_log_demo"
required-features = ["filesystem"]

[[example]]
name = "copyable_pane_demo"
